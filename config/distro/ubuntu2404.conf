# ubuntu2404.conf — Ubuntu 24.04 LTS overrides
# Sourced after master.conf when distro is detected as Ubuntu 24.04

DISTRO_ID="ubuntu2404"
DISTRO_LABEL="Ubuntu 24.04 LTS"

# Package manager
PKG_MANAGER="apt"

# Mandatory Access Control
MAC_SYSTEM="apparmor"

# Firewall
FIREWALL_SYSTEM="ufw"

# PAM configuration tool
PAM_TOOL="pam-auth-update"

# Time sync service (chrony preferred, systemd-timesyncd as fallback)
TIME_SYNC_SERVICE="chrony"
